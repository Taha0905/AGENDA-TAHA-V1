<UserControl x:Class="agenda_khelifi.View.ViewCalendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding Events}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Start Time" DisplayMemberBinding="{Binding Start.DateTime}" Width="150"/>
                    <GridViewColumn Header="End Time" DisplayMemberBinding="{Binding End.DateTime}" Width="150"/>
                    <GridViewColumn Header="Summary" DisplayMemberBinding="{Binding Summary}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
