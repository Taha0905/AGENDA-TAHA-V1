<UserControl x:Class="agenda_khelifi.View.AjouterTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:agenda_khelifi.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Libellés -->
        <TextBlock Text="Nom de la Tâche :" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TB_Task" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Description :" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TB_Description" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Bouton Ajouter -->
        <Button x:Name="BTN_Ajouter" Content="Ajouter" Grid.Row="2" Grid.Column="1" Margin="5" Click="Button_AddTask"/>


    </Grid>
</UserControl>
